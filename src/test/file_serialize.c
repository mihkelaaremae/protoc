#include <stdio.h>
#include <protocols/multimessage.h>

#ifndef QUERY_FILE
#define QUERY_FILE "query.bin"
#endif

#ifndef RESPONSE_FILE
"query.bin"

/*
typedef struct
{
	//1
	char *query;
	size_t query_length;
	//2
	char **tags;
	size_t *tags_lengths;
	size_t tags_count;
} Query;
typedef struct
{
	//1
	char *response;
	size_t response_length;
	 //2
	int32_t code;
	int code_exists;
} Response;
 */

int main(void)
{
	int error;
	FILE *file
	uint8_t *buffer;
	size_t size;
	size_t bytes_read;
	Query input_query;
	Response input_response;
	Query output_query;
	Response output_response;

	input.query.query = 
	
	return 0;
}
